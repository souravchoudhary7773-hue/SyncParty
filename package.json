{
  "name": "youtube-watch-party",
  "version": "1.0.0",
  "description": "Production-grade, highly scalable YouTube Watch Party real-time synchronization platform",
  "main": "server/dist/server.js",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "cd server && npm install --include=dev && npm run build",
    "build:client": "cd client && npm install --include=dev && npm run build",
    "build": "npm run build:server && npm run build:client",
    "start": "cd server && npm run start",
    "test:scale": "cd server && npx ts-node src/scripts/scaleTest.ts"
  },
  "keywords": [
    "youtube",
    "watch-party",
    "websockets",
    "socket.io",
    "scalability",
    "real-time",
    "rbac"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
